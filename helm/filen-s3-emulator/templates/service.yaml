apiVersion: v1
kind: Service
metadata:
  name: filen-s3-emulator-service
  labels:
    app: filen-s3-emulator
spec:
  selector:
    app: filen-s3-emulator
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
